# 测试用例生成器配置
# 将此文件复制为 .env 并填入你的配置值

# ============================================
# 工作流配置
# ============================================
# 是否启用需求分析节点（复杂需求时自动分析）
ENABLE_ANALYZER=false
# 复杂度阈值：需满足几个复杂度指标才触发分析（1-5）
# 指标包括：字符数>200、逗号>5个、包含复合词、包含问号、换行>3个
ANALYZER_COMPLEXITY_THRESHOLD=2
# 最大评审轮次（预留配置）
MAX_REVIEW_ROUNDS=1

# ============================================
# 节点零：需求分析器（可选）
# 仅在 ENABLE_ANALYZER=true 且需求复杂度达到阈值时生效
# 未配置时默认使用生成器的配置
# ============================================
ANALYZER_API_KEY=your-api-key-here
ANALYZER_BASE_URL=https://api.openai.com/v1
ANALYZER_MODEL_NAME=gpt-4o
ANALYZER_TEMPERATURE=0.3
ANALYZER_MAX_TOKENS=4096
# 推理力度（仅 reasoning 模型生效，如 gpt-5.4, o1 系列）
# 可选值：minimal, low, medium, high（留空则不传递，使用模型默认值）
# 注意：设置此项后 temperature 将被忽略
ANALYZER_REASONING_EFFORT=

# ============================================
# 节点一：生成器（测试用例生成）
# ============================================
GENERATOR_API_KEY=your-api-key-here
GENERATOR_BASE_URL=https://api.openai.com/v1
GENERATOR_MODEL_NAME=gpt-4o
GENERATOR_TEMPERATURE=0.7
GENERATOR_MAX_TOKENS=8192
# 推理力度（仅 reasoning 模型生效，如 gpt-5.4, o1 系列）
# 可选值：minimal, low, medium, high（留空则不传递，使用模型默认值）
# 注意：设置此项后 temperature 将被忽略
GENERATOR_REASONING_EFFORT=

# ============================================
# 节点二：评审员（测试用例评审）
# 建议使用更强大的推理模型
# ============================================
REVIEWER_API_KEY=your-api-key-here
REVIEWER_BASE_URL=https://api.openai.com/v1
REVIEWER_MODEL_NAME=o1-preview
REVIEWER_TEMPERATURE=1.0
REVIEWER_MAX_TOKENS=8192
# 推理力度（仅 reasoning 模型生效，如 gpt-5.4, o1 系列）
# 可选值：minimal, low, medium, high（留空则不传递，使用模型默认值）
# 注意：设置此项后 temperature 将被忽略
REVIEWER_REASONING_EFFORT=medium

# ============================================
# 节点三：优化器（测试用例优化）
# 建议与生成器使用相同模型
# ============================================
OPTIMIZER_API_KEY=your-api-key-here
OPTIMIZER_BASE_URL=https://api.openai.com/v1
OPTIMIZER_MODEL_NAME=gpt-4o
OPTIMIZER_TEMPERATURE=0.5
OPTIMIZER_MAX_TOKENS=8192
# 推理力度（仅 reasoning 模型生效，如 gpt-5.4, o1 系列）
# 可选值：minimal, low, medium, high（留空则不传递，使用模型默认值）
# 注意：设置此项后 temperature 将被忽略
OPTIMIZER_REASONING_EFFORT=

# ============================================
# RAG配置（可选）
# ============================================
RAG_ENABLED=false
RAG_COLLECTION_NAME=test_case_knowledge
RAG_EMBEDDING_MODEL=text-embedding-3-small
RAG_TOP_K=5

# ============================================
# 通用设置
# ============================================
# 测试用例分离模式：mixed（默认）或 frontend_backend（前后端分离）
TEST_CASE_SPLIT_MODE=mixed
# 前后端分离模式下是否严格校验结构并自动修复
TEST_CASE_SPLIT_STRICT=true
USE_RESPONSES_API=true
REQUEST_TIMEOUT=120
MAX_RETRIES=3
